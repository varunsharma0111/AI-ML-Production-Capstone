apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: capstone-dev

resources:
  - "../../base"

patches:
  - target:
      kind: Deployment
      name: api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
      kind: Deployment
      name: web
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
